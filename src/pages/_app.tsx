import "../styles/global.css";



export function MyApp({ Component, pageProps }) {
  return (
        <Component {...pageProps} />
  );
}

export default MyApp;

// Fazer documentação
// Dark Mode
// Responsividade
// missionComplete